class Cast {
    constructor(id, character, name, profileUrl) {
        (this.id = id),
        (this.character = character),
        (this.name = name),
        (this.profileUrl = profileUrl);
    }
}

export default Cast;