class Story {
  constructor(id, title, genres, language, movieLink) {
    (this.id = id),
      (this.title = title),
      (this.genres = genres),
      (this.language = language),
      (this.movieLink = movieLink);
  }
}

export default Story;
